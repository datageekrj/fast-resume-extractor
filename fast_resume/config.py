# Default threshold for skill matching
DEFAULT_SIMILARITY_THRESHOLD = 0.1

# Default ngram range for TF-IDF
DEFAULT_NGRAM_RANGE = (3, 5)

# Default path to skill file
DEFAULT_SKILL_FILE = "data/skills.json"

# Package version
__version__ = "0.1.0"
